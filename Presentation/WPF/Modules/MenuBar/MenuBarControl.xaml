<UserControl x:Class="Zander.Modules.MenuBar.MenuBarControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d">
	<Menu>
		<MenuItem Header="{Binding Path=Strings.File}">
			<MenuItem Header="{Binding Path=Strings.Quit}" Command="{Binding Path=Actions.Quit}" />
		</MenuItem>
		<MenuItem Header="{Binding Path=Strings.Edit}">
			
		</MenuItem>
		<MenuItem Header="{Binding Path=Strings.Server}">
			<MenuItem Header="{Binding Path=Strings.Refresh}" Command="{Binding Path=Actions.Refresh}" />
			<MenuItem Header="{Binding Path=Strings.AddToIgnoreList}" Command="{Binding Path=Actions.AddToIgnoreList}" />
			<MenuItem Header="{Binding Path=Strings.EditCustomServers}" Command="{Binding Path=Actions.EditCustomServers}" />
			<MenuItem Header="{Binding Path=Strings.EditIgnoreList}" Command="{Binding Path=Actions.EditIgnoreList}" />
		</MenuItem>
		<MenuItem Header="{Binding Path=Strings.Filter}">

		</MenuItem>
		<MenuItem Header="{Binding Path=Strings.Help}">

		</MenuItem>
	</Menu>
</UserControl>
